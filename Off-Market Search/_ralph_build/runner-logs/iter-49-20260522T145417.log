Iteration 80 complete — **RESOLVE phase**, resolved finding **NIT-s9-2**.

**What I did this step:**
- All 10 stages are `verified` and `final_audit_passed: true`, but `unresolved_findings: 3` — so the loop is in RESOLVE, clearing one IMPROVE/NIT finding per iteration.
- Picked **NIT-s9-2** (dry-run run log omits enrichment-only sources). The root fix belongs in the *spec* that governs live runs, not the frozen SELF-TEST fixture:
  - `orchestration.md` §3 — added a directive comment + a paragraph requiring the run-log "Sources queried" table to list **every** source attempted, discovery adapters (S1–S4, S8) *and* enrichment-only sources (S5, S9, S10, S11), including 0-record/skipped ones; added S5/S9 example rows; genericised the stale `blocked (B3)` example row to a non-blocker `degraded` (HTTP 429) example.
  - `s9-offmarket_run_log_dryrun.md` — added an iter-80 note explaining the omission is acceptable for a frozen artifact and the live-run requirement is now enforced by the template.
- Moved NIT-s9-2 to FINDINGS.md "Resolved" with full resolution text.
- `STATE.md`: `iteration` 79→80, `last_iteration_at` updated, `unresolved_findings` 3→2.
- Committed locally as `offmarket-build: iter 80 — RESOLVE NIT-s9-2`.

**Signal: `CONTINUE`** — 2 non-blocked findings remain (IMPROVE-s5-3, IMPROVE-s6-2), `open_blockers: 0`, `iteration` 80 < `max_iterations` 120.
