Iteration 17 complete — one step done.

**Step executed:** s6 (Scoring integration) — IMPLEMENT phase (`not_started → drafted`).

**Built:**
- `references/scoring_integration.md` — the s6 integration layer that scores each s5 `LeadPacket` by invoking the **unmodified** `prospect-evaluation` skill. Defines the `ScoredLead` output object, per-class mode selection (`rollup_addon` for Class 1 — Applied Development, /110, no size floor, adjacent-tier bonus 5-not-10; `sbic` for Class 2 — license-good-standing gate, informational score), the `LeadPacket`→scorer field mapping, graceful "no asking price" handling ("insufficient data — not awarded", not a failure/abort), the SBIC `buybox_gate` derivation, report capture, and a filesystem-safe `report_slug`.
- `skill.md` Step 5 updated to point at the new reference.

**State:** iteration 17/80; s6 `drafted`; open_blockers 4, unresolved_findings 15 (unchanged). Committed locally as `543f6ac`.

**Stop checks:** not COMPLETE; under max_iterations; s6–s10 remain non-blocked — so the loop continues.

**Signal:** `CONTINUE` — next iteration runs s6 SELF-TEST.
