Iteration 21 complete — **one step: s7 (Airtable write & dashboard badge) SELF-TEST**.

**Results:**
- **C1–C4 PASS** — the `.chip.offmarket` style block is well-formed; a live Jinja2 render confirmed the badge fires only on `Off-Market — …` sources (not on `Overnight Search`/`Manual Submission`); all ~30 field IDs in `airtable_write.md` §3 cross-checked valid (the five §8.4 fields confirmed live via `get_table_schema` with correct types); create/update split and no-fabrication logic verified by inspection.
- **C5 BLOCKED by B4** — a live read of the `Source` field shows only `["Overnight Search", "Manual Submission"]`; the two off-market values are still absent. The Step-1 preflight fails-loud and halts before s7 runs, and §2/§6 forbid auto-creating the value, so a live off-market row cannot be written. No write was attempted (it would fail or fabricate).

**State changes:**
- s7 → `blocked` (B4); the central s7 `Done-when` (a scored prospect appearing as a live row) can't be met while B4 is open.
- B4 updated — now records it blocks **both s2 and s7**; retries once the operator adds the two `Source` single-select values.
- STATE bumped to iter 21; `open_blockers` stays 4, `unresolved_findings` 19.
- Committed `422bcce`.

**Signal: CONTINUE** — s8 (outreach drafting) is still non-blocked, so the loop proceeds there next iteration.
