Iteration 8 complete.

**Step executed:** s4 (Entity resolution & de-duplication) — IMPLEMENT phase (`not_started → drafted`).

s4 was the lowest-numbered non-verified, non-blocked stage (s2 is blocked on B4; s1/s3 verified).

**Built:**
- `references/entity_resolution.md` — the resolver + tracker dedup spec implementing PRD §6.1/§6.2: the `CanonicalEntity` schema, the UEI → CAGE → legacy DUNS → name+address key ladder, the three-key tracker dedup (gov ID / name+address / SBIC license #), cross-run dedup via the tracker itself, fail-loud handling on a tracker read failure, and the §2.2 accuracy spot-check.
- `skill.md` Step 3 updated to point at the new reference.

**State:** iteration 8; s4 → `drafted`; `open_blockers: 4`, `unresolved_findings: 4` unchanged. Committed locally (`offmarket-build: iter 8 — s4 entity_resolution IMPLEMENT`). Signal: **CONTINUE** — s4 SELF-TEST and downstream non-blocked work remain.
