Iteration 36 complete — one step: **s7 (Airtable write & dashboard badge) — SELF-TEST**.

**What happened:** With B4 resolved, I drove the `airtable_write.md` write procedure for real against the live Master Deal Pipeline base, attempting an actual create of the Class-2 SBIC lead R2 (1st Source Capital Corporation, 20/100).

- Preflight passed (all six §8.4 fields + the two off-market `Source` values confirmed live).
- Dedup passed (R2 is `new` — zero existing rows).
- Badge checks C1–C4 re-confirmed.
- **C5 FAILED.** The live `create_records_for_table` was rejected atomically — `HTTP 422: Insufficient permissions to create new select option "SBA SBIC directory"`. Root cause: `airtable_write.md` §3.1 maps `Lead Source` to a free-text gov-source string, but `Lead Source` is a **singleSelect** restricted to 14 broker-platform options. No record was written (confirmed by a follow-up search).

This is a genuine defect in the s7 deliverable, not an external blocker. Per the SELF-TEST contract a failed check returns the stage to IMPLEMENT.

**State changes:**
- `s7_airtable_write`: `drafted` → `not_started`
- New finding **BLOCKING-s7-1** logged (`unresolved_findings` 27 → 28)
- `iteration` 35 → 36; committed locally; signal `CONTINUE`

The fix for the next IMPLEMENT: leave `Lead Source` blank for off-market rows (gov provenance is already carried by the dedicated `Gov Data Source` multi-select and `Links`), or map to an existing option — then re-run the SELF-TEST with the live R2 write.
