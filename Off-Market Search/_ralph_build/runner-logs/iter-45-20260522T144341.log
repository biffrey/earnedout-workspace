Iteration 76 complete — RESOLVE phase, cleared one finding.

**Step taken:** RESOLVE — NIT-s9-1 (`dedup_verdict` enum vs. §1 hand-off table)

The §1 stage hand-off table in `orchestration.md` listed Step 3's output as tagged `new / existing / needs_operator_review`, presenting `needs_operator_review` as if it were a third `dedup_verdict` enum value — but `entity_resolution.md:61` types `dedup_verdict` as only `new | existing`, and `needs_operator_review` is a §4 run-log exclusion status, not a verdict.

**Fix:** reworded the Step 3 "Produces" cell and added a `[†]` footnote clarifying that Step 3 yields exactly two verdict values, that only entities surviving §2 resolution get one, and that `needs_operator_review` entities are excluded before §3 dedup and never passed downstream. Aligns `orchestration.md` with the `entity_resolution.md:61` note from the IMPROVE-s4-1 resolution. Spec-text clarity only — no behavior change.

**State:** iteration 75 → 76, `unresolved_findings` 7 → 6. All 10 stages remain `verified`, `final_audit_passed: true`, `open_blockers: 0`. Committed locally as `iter 76 — RESOLVE NIT-s9-1`.

**Signal:** `CONTINUE` — 6 IMPROVE/NIT findings remain in the RESOLVE phase.
