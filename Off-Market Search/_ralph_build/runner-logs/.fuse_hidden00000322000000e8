Iteration 78 complete — one RESOLVE step done.

**Step executed:** RESOLVE phase (all 10 stages `verified`, `final_audit_passed: true`, `open_blockers: 0`) — cleared finding **IMPROVE-s8-1**.

**What changed:** The OM-2 SBIC outreach template assumed the recipient was a GP principal, but the s8 contact gate accepts any direct contact (e.g. an "Investor Relations" contact). I softened the wording so the body is role-agnostic:
- `config/offmarket_outreach_template.md` — OM-2 body ("as a principal" → "directly"; "you" → "the firm's leadership"), the OM-2 intro paragraph, the `[PRINCIPAL_NAME]` placeholder note, and a new Tone Guidance item 7.
- `references/outreach_drafting.md` — a new §5 "Non-principal Class-2 contact" edge bullet.

Tone/spec-clarity only — no contact gate, template selection, storage, or no-send behavior changed; s8 stays `verified`.

**State updated:** `iteration` 77 → 78, `unresolved_findings` 5 → 4. Logged to `IMPLEMENTATION_LOG.md`, finding moved to `FINDINGS.md` Resolved, committed locally.

**Remaining findings (4):** IMPROVE-s5-3, IMPROVE-s6-2, NIT-s9-2, NIT-s10-1.

**Signal:** `CONTINUE` — RESOLVE phase continues; non-blocked work remains.
