Iteration 22 complete — one (stage, phase) step done.

**Step executed:** s8 (Outreach drafting) — **IMPLEMENT** phase.

**What was built:**
- `config/offmarket_outreach_template.md` — a **new sibling file** (broker templates in `config/outreach_templates.md` left fully untouched, per the s8 Done-when). Contains **Template OM-1** (Class-1 ASL/CART owner approach) and **Template OM-2** (Class-2 SBIC GP principal approach) — proprietary-approach tone, no NDA/CIM language, never-fabricate placeholder rules, A/B subject rotation. OM-2 carries the SBA-prior-approval fact as fixed body text.
- `.claude/skills/off-market-search/references/outreach_drafting.md` — the s8 Step-7 procedure: contact gate (no contact → no draft), template/variant selection, the `NOT SENT` draft-block format, two-place storage (`Notes` + `search_reports/offmarket_outreach_drafts_YYYY-MM-DD.md`), edge handling.
- `skill.md` Step 7 wired to the new reference and template.

**State updates:** iteration 21→22; s8 `not_started → drafted`. Blockers unchanged (B1–B4 still open; s2/s7 still `blocked` on B4). `open_blockers: 4`, `unresolved_findings: 19`.

**Committed:** `76056cd`. **Signal:** `CONTINUE` — s8 SELF-TEST and stages s9/s10 remain as non-blocked work.
