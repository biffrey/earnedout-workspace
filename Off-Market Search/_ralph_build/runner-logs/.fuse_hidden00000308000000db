Iteration 69 complete — **RESOLVE phase**, one finding cleared.

**Resolved IMPROVE-s3-2** — the S8 state-source adapter (priority-state eProcurement portals + Secretary-of-State registries) was a fixture-shell because s3 was verified while blocker B1 was open. B1 is now resolved (Phase-1 jurisdictions: DC, VA, MD, PA, WV), so I rebuilt it into a working adapter:

- **`source_adapters.md`** — rewrote the S8 section: per-jurisdiction portal table (eProcurement + SOS registry hosts for each of the five jurisdictions), a B1-mandated per-jurisdiction ToS-confirmation gate (honor `robots.txt`, read Terms of Use, prefer official exports over scraping, skip → `degraded` any unconfirmed jurisdiction), query/map logic, and `ok`/`degraded`/`error` status — never `blocked`. Registry row and the `blocker_id` example updated.
- **`S8.json`** — new structural fixture, one sample record per jurisdiction; fixtures `README.md` updated.
- **`config/offmarket_sources.md`** — S8 entry status `blocked by B1` → `not blocked (B1 resolved)` with the five portals listed.

No live state-portal call was made (each portal's ToS must be confirmed first — the B1 mandate — which a headless run can't complete); the adapter is spec-complete with documented ToS gate + fixture fallback. Common adapter interface unchanged, so s4–s6 are unaffected.

State updated: `iteration: 69`, `unresolved_findings: 14 → 13`. Committed as `8436b15`. Signal: **CONTINUE** — 13 findings remain in the RESOLVE phase.
