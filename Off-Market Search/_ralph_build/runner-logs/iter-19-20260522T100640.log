Iteration 20 complete.

**Step executed:** s7 (Airtable write & dashboard badge) — IMPLEMENT (`not_started → drafted`)

s7 was the lowest-numbered non-verified, non-blocked stage (s2 is blocked on B4; s3–s6 verified).

**Built:**
- `references/airtable_write.md` — the field-by-field PRD §8 mapping for record create (§3: existing fields, the 16 reused fields, the five new §8.4 fields, each with its live field ID) and update-in-place for `existing` entities (§4); fail-loud preconditions (§2); the `.chip.offmarket` badge spec (§5); failure/edge handling (§6).
- `templates/daily-dashboard.html` — new `.chip.offmarket` style and an additive `OFF-MARKET` badge rendered on rows where `lead.source` starts with `"Off-Market"`, in Sections A/B/C. On-market rows render exactly as before.
- `skill.md` Steps 6 and 8 repointed at the new reference.

**State:** iteration 20, s7 → `drafted`; `open_blockers: 4`, `unresolved_findings: 19` (unchanged). B4 does not block this IMPLEMENT — the mapping reference and badge are static deliverables; the live record write gets exercised in s7 SELF-TEST.

**Signal:** `CONTINUE` — non-blocked work remains (s7 SELF-TEST/VERIFY, s8, s9, s10). Committed locally as `94cc8ac`.
